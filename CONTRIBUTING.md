# Contributing

1. Fork the repository and create a focused branch.
2. Keep connector and filter APIs small and testable.
3. Do not commit commercial ROMs, credentials, or private signing keys.
4. Run `npm run check` before opening a pull request.
5. Describe ABI changes explicitly in the PR body.

For emulator-core changes, include regression tests or public/homebrew test-ROM
results where possible.
